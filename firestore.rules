rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Segments collection - allow all reads and writes
    // Since we're using a simple password gate on the client,
    // we allow anyone to read/write once they have access to the app
    match /segments/{document=**} {
      allow read, write: if true;
    }
  }
}
